<div id="social-box">
	{{#each model as |social|}}
	  <a href="{{social.link}}"><h4><button class="btn btn-link" href="{{social.link}}">{{fa-icon social.icon}} </button>{{social.name}}</h4></a>
	{{/each}}
</div>