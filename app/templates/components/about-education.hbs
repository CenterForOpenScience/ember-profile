{{#cp-panels accordion=true}}
  {{#each model as |educator|}}
    <div>
      {{#cp-panel}}
        {{#cp-panel-toggle}}
          <div class="drop-down">
            <aside>
              <h4>{{educator.name}}</h4>
            </aside>
            {{fa-icon "chevron-right" size=1}}
          </div>
        {{/cp-panel-toggle}}
        {{#cp-panel-body}}
          <dl class="dl-horizontal">
            <dt>Department:</dt>
            <dd>{{educator.department}}</dd>
            <dt>Degree:</dt>
            <dd>{{educator.degree}}</dd>
            <dt>Dates:</dt>
            <dd>{{educator.dates}}</dd>
          </dl>
        {{/cp-panel-body}}
      {{/cp-panel}}
    </div>
  {{/each}}
{{/cp-panels}}