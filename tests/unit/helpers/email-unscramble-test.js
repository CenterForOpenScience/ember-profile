import { emailUnscramble } from 'osf-profile/helpers/email-unscramble';
import { module, test } from 'qunit';

module('Unit | Helper | email unscramble');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = emailUnscramble([42]);
  assert.ok(result);
});
