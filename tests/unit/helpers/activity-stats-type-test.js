import {activityStatsType} from "osf-profile/helpers/activity-stats-type";
import {module, test} from "qunit";

module('Unit | Helper | activity stats type');

// Replace this with your real tests.
test('it works', function (assert) {
    let result = activityStatsType([42]);
    assert.ok(result);
});
