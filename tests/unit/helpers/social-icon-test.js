import { socialIcon } from 'osf-profile/helpers/social-icon';
import { module, test } from 'qunit';

module('Unit | Helper | social icon');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = socialIcon([42]);
  assert.ok(result);
});
