import {socialUsername} from "osf-profile/helpers/social-username";
import {module, test} from "qunit";

module('Unit | Helper | social username');

// Replace this with your real tests.
test('it works', function (assert) {
    let result = socialUsername([42]);
    assert.ok(result);
});
